<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Undangan Prewedding - Rizal & Selvi</title>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Montserrat:wght@300;400;500;600&family=Sacramento&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #0b1a30;
            --card-bg: #132743;
            --accent-gold: #d4af37;
            --text-light: #f0f4f8;
            --text-muted: #a0aec0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-light);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .mobile-container {
            width: 100%;
            max-width: 450px;
            background-color: var(--bg-color);
            min-height: 100vh;
            position: relative;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
            overflow-x: hidden;
        }

        /* Hero / Header Section */
        .hero {
            position: relative;
            text-align: center;
            padding: 50px 20px 30px;
            background: linear-gradient(180deg, rgba(11,26,48,0.3) 0%, rgba(11,26,48,1) 100%), 
                        url('https://images.unsplash.com/photo-1519741497674-611481863552?q=80&w=1000&auto=format&fit=crop') center/cover;
        }

        .subtitle {
            font-family: 'Cinzel', serif;
            font-size: 14px;
            letter-spacing: 3px;
            color: var(--accent-gold);
            margin-bottom: 15px;
            text-transform: uppercase;
        }

        .couple-names {
            font-family: 'Sacramento', cursive;
            font-size: 52px;
            color: #ffffff;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
        }

        .date {
            font-family: 'Montserrat', sans-serif;
            font-size: 14px;
            letter-spacing: 1px;
            color: var(--text-muted);
        }

        /* Photo Banner */
        .photo-banner {
            width: 100%;
            padding: 20px;
            text-align: center;
        }

        .photo-frame {
            width: 100%;
            border-radius: 15px;
            border: 2px solid var(--accent-gold);
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0,0,0,0.4);
        }

        .photo-frame img {
            width: 100%;
            display: block;
        }

        /* Countdown */
        .countdown-section {
            padding: 30px 20px;
            text-align: center;
        }

        .section-title {
            font-family: 'Cinzel', serif;
            font-size: 20px;
            color: var(--accent-gold);
            margin-bottom: 20px;
            letter-spacing: 2px;
        }

        .timer {
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        .timer-box {
            background: var(--card-bg);
            border: 1px solid rgba(212, 175, 55, 0.3);
            border-radius: 10px;
            padding: 12px;
            min-width: 70px;
        }

        .timer-num {
            font-size: 22px;
            font-weight: 600;
            color: var(--accent-gold);
        }

        .timer-label {
            font-size: 10px;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-top: 4px;
        }

        /* Event Details */
        .event-section {
            padding: 30px 20px;
            text-align: center;
        }

        .event-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 25px 20px;
            border: 1px solid rgba(212, 175, 55, 0.2);
            margin-bottom: 20px;
        }

        .event-title {
            font-family: 'Cinzel', serif;
            font-size: 18px;
            color: var(--accent-gold);
            margin-bottom: 15px;
        }

        .event-info {
            font-size: 14px;
            line-height: 1.8;
            color: var(--text-light);
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            background: linear-gradient(135deg, #d4af37 0%, #aa820a 100%);
            color: #fff;
            padding: 12px 25px;
            border-radius: 25px;
            text-decoration: none;
            font-size: 13px;
            font-weight: 600;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(212, 175, 55, 0.5);
        }

        /* RSVP Form */
        .rsvp-section {
            padding: 30px 20px 50px;
            text-align: center;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-size: 12px;
            color: var(--text-muted);
            margin-bottom: 5px;
        }

        .form-control {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 1px solid rgba(212, 175, 55, 0.3);
            background: var(--card-bg);
            color: #fff;
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent-gold);
        }

        footer {
            text-align: center;
            padding: 20px;
            font-size: 12px;
            color: var(--text-muted);
            border-top: 1px solid rgba(255,255,255,0.05);
        }
    </style>
</head>
<body>

    <div class="mobile-container">
        <!-- Header -->
        <div class="hero">
            <div class="subtitle">The Wedding Of</div>
            <div class="couple-names">Rizal & Selvi</div>
            <div class="date">Jumat, 11 September 2026</div>
        </div>

        <!-- Foto Banner -->
        <div class="photo-banner">
            <div class="photo-frame">
                <!-- Gantilah URL gambar di bawah dengan foto prewedding Anda -->
                <img src="foto-prewedding.jpg" alt="Rizal & Selvi">
            </div>
        </div>

        <!-- Countdown -->
        <div class="countdown-section">
            <div class="section-title">Menuju Hari Bahagia</div>
            <div class="timer">
                <div class="timer-box">
                    <div class="timer-num" id="days">00</div>
                    <div class="timer-label">Hari</div>
                </div>
                <div class="timer-box">
                    <div class="timer-num" id="hours">00</div>
                    <div class="timer-label">Jam</div>
                </div>
                <div class="timer-box">
                    <div class="timer-num" id="minutes">00</div>
                    <div class="timer-label">Menit</div>
                </div>
                <div class="timer-box">
                    <div class="timer-num" id="seconds">00</div>
                    <div class="timer-label">Detik</div>
                </div>
            </div>
        </div>

        <!-- Event Detail -->
        <div class="event-section">
            <div class="event-card">
                <div class="event-title">Acara Pernikahan</div>
                <div class="event-info">
                    <strong>Waktu:</strong> 10:00 WIB - Selesai<br>
                    <strong>Tempat:</strong> Hotel Aston, Banjarnegara
                </div>
                <a href="https://maps.google.com" target="_blank" class="btn">Petunjuk Lokasi (Google Maps)</a>
            </div>
        </div>

        <!-- RSVP Form -->
        <div class="rsvp-section">
            <div class="section-title">Konfirmasi Kehadiran</div>
            <form id="rsvpForm">
                <div class="form-group">
                    <label>Nama Lengkap</label>
                    <input type="text" id="nama" class="form-control" placeholder="Masukkan nama Anda" required>
                </div>
                <div class="form-group">
                    <label>Jumlah Tamu</label>
                    <input type="number" id="jumlah" class="form-control" placeholder="1" min="1" required>
                </div>
                <div class="form-group">
                    <label>Konfirmasi Kehadiran</label>
                    <select id="status" class="form-control" required>
                        <option value="Hadir">Saya Akan Hadir</option>
                        <option value="Tidak Hadir">Maaf, Tidak Bisa Hadir</option>
                    </select>
                </div>
                <button type="button" class="btn" onclick="sendToWA()">Kirim Konfirmasi Via WhatsApp</button>
            </form>
        </div>

        footer {
            text-align: center;
            padding: 20px;
            font-size: 12px;
            color: var(--text-muted);
            border-top: 1px solid rgba(255,255,255,0.05);
        }
    </div>

    <script>
        // Countdown Timer Script
        const targetDate = new Date("September 11, 2026 10:00:00").getTime();

        const countdown = setInterval(function() {
            const now = new Date().getTime();
            const distance = targetDate - now;

            const days = Math.floor(distance / (1000 * 60 * 60 * 24));
            const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
            const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            const seconds = Math.floor((distance % (1000 * 60)) / 1000);

            document.getElementById("days").innerText = days < 10 ? "0" + days : days;
            document.getElementById("hours").innerText = hours < 10 ? "0" + hours : hours;
            document.getElementById("minutes").innerText = minutes < 10 ? "0" + minutes : minutes;
            document.getElementById("seconds").innerText = seconds < 10 ? "0" + seconds : seconds;

            if (distance < 0) {
                clearInterval(countdown);
                document.querySelector(".timer").innerHTML = "<h3>Acara Telah Berlangsung</h3>";
            }
        }, 1000);

        // RSVP WhatsApp Sender
        function sendToWA() {
            const nama = document.getElementById("nama").value;
            const jumlah = document.getElementById("jumlah").value;
            const status = document.getElementById("status").value;
            
            // Ganti nomor WhatsApp tujuan di bawah (Gunakan format 62...)
            const noWA = "628123456789";

            if (nama === "") {
                alert("Silakan isi nama Anda terlebih dahulu.");
                return;
            }

            const text = `Halo, saya *${nama}*.%0A%0AConfirmasi Kehadiran Undangan Rizal %26 Selvi:%0A- Status: *${status}*%0A- Jumlah Tamu: *${jumlah} orang*%0A%0ATerima Kasih.`;
            window.open(`https://wa.me/${noWA}?text=${text}`, '_blank');
        }
    </script>
</body>
</html>
